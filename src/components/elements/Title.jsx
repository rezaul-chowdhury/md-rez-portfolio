import React from 'react';

const Title = (props) => {
    return (
        <div>
            <h1 className="font-Tangerine text-5xl">{props.title}</h1>
        </div>
    );
};

export default Title;
